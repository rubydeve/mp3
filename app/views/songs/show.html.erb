<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @song.title %>
</p>





<br>
<div>
 <audio controls>
  <source src="<%= song_live_song_url(@song.id) %>" type="audio/ogg">
  <source src="<%= song_live_song_url(@song.id) %>" type="audio/mpeg">
  Your browser does not support the audio tag.
</audio> 
</div>

<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>

